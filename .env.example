# Frontend API base URL. In local development we recommend using /api together
# with the Vite proxy below.
VITE_API_BASE_URL="/api"

# Local Vite proxy target for the Python backend.
VITE_API_PROXY_TARGET="http://127.0.0.1:8000"
